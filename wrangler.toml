# Cloudflare Pages (Git) — ONLY keys Pages accepts.
# Do not add [build] or [assets] here; Pages rejects them and the deploy fails.
# For Workers static-asset deploy, use wrangler.worker.toml instead.

name = "microbudgetfilmmaking"
pages_build_output_dir = "nextjs-site/out"
